import React from 'react';

interface FloatingCTAProps {
  onClickCTA: () => void;
}

// Componente simplificado: retorna null para remover o botão flutuante
const FloatingCTA: React.FC<FloatingCTAProps> = () => {
  // Retorna null para remover completamente o botão flutuante
  return null;
};

export default FloatingCTA;
